# デプロイ手順書（GitHub Pages）

このアプリは静的サイトです。ビルドは不要で、そのまま置けば動きます。
データはGoogleスプレッドシートから毎回読みに行くので、**シートを更新してもデプロイし直す必要はありません**。

公開に必要なファイルは2つです。

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。これ1枚で全ページが動く |
| `map-data.js` | 地図ページの下絵（約80KB）。**地図を開いたときだけ**読み込む |
| `robots.txt` | 検索エンジンに載せないための指示 |

`tools/` は下絵を作り直すためのもので、公開先に置く必要はありません。

---

## 初回だけ行う作業

### 1. GitHubでリポジトリを作る

https://github.com/new を開いて、次のとおり設定します。

| 項目 | 値 |
|---|---|
| Repository name | `winning-post-app`（好きな名前で可） |
| Public / Private | **Public** |
| Add a README file | **チェックしない** |
| .gitignore / license | **どちらも None** |

> **Publicにする理由**：GitHubの無料プランでは、Privateリポジトリで GitHub Pages を公開できません（Pro以上が必要）。
> なお、対象のスプレッドシートは現時点でリンクを知っていれば誰でも閲覧できる状態なので、
> 公開範囲は実質的に変わりません。気になる場合は Cloudflare Pages などPrivate前提の選択肢に切り替えられます。

「Create repository」を押すと、次のページに `https://github.com/<ユーザー名>/winning-post-app.git` というURLが表示されます。これを控えてください。

### 2. ローカルからpushする

`C:\WinningPostApp` で以下を実行します。`<ユーザー名>` は自分のGitHubアカウント名に置き換えてください。

```bash
git remote add origin https://github.com/<ユーザー名>/winning-post-app.git
git push -u origin main
```

初回は認証を求められます。ブラウザが開いたらGitHubにログインして許可してください。

### 3. GitHub Pages を有効にする

1. リポジトリのページで **Settings** タブを開く
2. 左メニューの **Pages** をクリック
3. **Build and deployment** の **Source** で `Deploy from a branch` を選ぶ
4. **Branch** を `main` ／ フォルダを `/ (root)` にして **Save**

1〜3分ほどで、同じPagesの画面の上部に公開URLが表示されます。

```
https://<ユーザー名>.github.io/winning-post-app/
```

### 4. スマホのホーム画面に追加する

上のURLをスマホのブラウザで開き、

- **iPhone (Safari)** … 共有ボタン → 「ホーム画面に追加」
- **Android (Chrome)** … 右上の︙ → 「ホーム画面に追加」

アプリのように起動できるようになります。

---

## 普段の運用

### データを増やしたとき

**何もしなくて構いません。** スプレッドシートに行を追加すれば、次にページを開いたときに反映されます。

- 初回表示：6シートを並列取得して約0.7秒
- 2回目以降：前回のデータを `localStorage` から即座に描画し、裏側で最新を取得して差し替え

すぐに反映されないときは、ブラウザでスーパーリロード（`Ctrl + Shift + R`）してください。

### アプリの見た目や集計ルールを変えたとき

`index.html` を編集して push するだけです。1〜2分でPagesに反映されます。

```bash
git add -A
git commit -m "変更内容の説明"
git push
```

---

## ローカルで動作確認する

`file://` で直接開くと**動きません**。GoogleがCORSヘッダを返さないためです。
必ずHTTPサーバー経由で開いてください。

```bash
python -m http.server 4173
```

その後 http://localhost:4173 を開きます。
（Claude Code内では `.claude/launch.json` の `winningpost` 設定で同じことができます）

---

## 設定を変えたい場合

`index.html` の冒頭付近、`/* ============ 設定 ============ */` の下にまとまっています。

| 定数 | 既定値 | 意味 |
|---|---|---|
| `SHEET_ID` | `11lAW1NI…` | 読み込むスプレッドシートのID |
| `SHEETS` | — | 各シートのgidと絞り込みクエリ |
| `PT_G1` | `20000` | 複合スコアでG1 1勝に加算する点数 |
| `PT_JPN1` | `10000` | 同じくJpnⅠ 1勝ぶんの点数 |
| `OVERSEAS_MIN_G1` | `2` | 海外部門で無条件受賞になるG1勝ち数 |
| `OVERSEAS_MIN_PRIZE` | `10000` | G1 1勝のときに海外部門で必要な年間賞金 |
| `SPRINT_MAX` | `1400` | 最優秀スプリンターの対象距離の上限 |
| `MILE_MAX` | `1600` | 最優秀マイラーの対象距離の上限（下限は`SPRINT_MAX`+1） |
| `HAS_SPRINT_MILE` | 日本のみ`true` | スプリンター・マイラー部門を設ける地域 |
| `AGE_LIMITED` | 4レース | 古馬混合かの判定で除外する2歳・3歳限定G1 |
| `RATE` | — | 着順別の賞金率（`n`=中央/海外、`j`=地方交流） |
| `ALIAS` | — | 表記ゆれの名寄せ表 |

**シートを増やしたとき**は `SHEETS` に `{ key:'名前', gid:数値 }` を足します。
gidはスプレッドシートでそのシートを開いたときのURL末尾 `#gid=……` の数値です。

### 配合分析の牝系判定

血統ページの配合分析は、**父 × 母の牝系**で集計します。牝系は母をたどれるだけたどった
先頭の馬（tail-female の根）です。母が所有馬として登録されていない場合は、その母の名前を
牝系名として扱います（外部の繁殖牝馬は所有していないため、そこで止まります）。

全190通りのうち、2頭以上の産駒がいる配合は18通りです。既定では2頭以上のみを表示し、
チェックを外すと1頭だけの配合も含めた全件が出ます。

### 地図ページ

競馬場ごとの成績を、緯度経度で地図に並べた積み上げ棒で表示します。
表示は**世界・日本・アメリカ・欧州**の4面、指標は**勝利数・出走数・獲得賞金・G1勝ち**、
内訳は**馬場別（芝／ダート）**か**格別（G1・重賞・その他）**を選べます。

- 棒の高さは、その面に出ている競馬場のうち最大のものを基準にした相対値です
- 棒は平面（上端だけ角丸）です。立体の柱も試しましたが、読みにくいので戻しました
- 奥（地図の上）から手前の順に描くので、重なったときは手前の棒が上になります
- 近い競馬場（東京・中山・大井・川崎など）は棒を横にずらし、細い引き出し線で本来の位置を指します。
  世界地図は日本に20場が集まって場所が足りないため、柱を細くし間隔も詰めています（`wide`）
- 直接ラベルは大きい順に置き、重なるものは省きます。省かれた競馬場は隣の表で確認できます
- 選んだ指標が0の競馬場は表示されません（勝利数で見ると未勝利の競馬場が消えます）

### 名馬列伝

記録から自動で経歴文を組み立てるページです。掲載の条件は
**G1・JpnⅠを3勝以上している**こと（`LEGEND_MIN_G1`）。現在42頭が該当します。

文章は `legendProse()` が事実を並べて作ります。**情報が欠けている馬では文ごと落とす**
作りなので、父母が未登録でも産駒がいなくても文が壊れません。重複した称号は年をまとめ、
連続受賞は「◯年から3年続けて」とまとめます。全勝の馬には連勝数の文を出しません
（「一度も敗れることなく」と重なるため）。

**対象は所有馬だけです。** 他厩舎の馬は勝ち鞍しか分からないので経歴を書けません。

### コース形態（`COURSE_INFO`）

競馬場ページに「右回り／左回り」と特徴を表示します。**スプレッドシートには無い情報**なので、
`index.html` の `COURSE_INFO` に実在の競馬場の作りを手で入れています。
ゲーム内の設定と食い違う場合はここを直してください。画面にもその旨を注記しています。

座標は `tools/venues.json`、コース形態は `index.html` と置き場所が分かれています。
前者は下絵の生成に、後者は競馬場ページの表示に使うためです。競馬場を増やすときは両方に足します。

**競馬場を増やしたとき**は `tools/venues.json` に `"◯◯競馬場":[緯度, 経度]` を足して、
下絵を作り直します。座標が無い競馬場は地図に出ず、ページ下の注記に名前が並びます。

```bash
node tools/build-map-data.mjs
```

**作り直したら `index.html` の `MAP_DATA_VER` を必ず上げてください。**
GitHub Pages は `Cache-Control: max-age=600` を返すため、`index.html` が新しくても
ブラウザが古い `map-data.js` を握り続けることがあります。読み込みURLに `?v=` を付けて
いるので、この数字を変えれば確実に新しい下絵が読まれます。

このスクリプトは Natural Earth（world-atlas の110m・50m）をCDNから取得し、
Webメルカトルに投影して Douglas-Peucker で間引き、`map-data.js` を上書きします。
表示範囲や粗さを変えたいときは、スクリプト末尾の `VIEWS` を編集してください
（`[データ, [西経度,南緯度,東経度,北緯度], 幅, 最小面積, 小数桁, 間引きの強さ]`）。

**表示範囲の外は Sutherland-Hodgman で切り落とします**（`clipPoly`）。
以前は範囲外の点を縁に貼り付けていたため、図形が画面を横切る線になって現れていました。

**日付変更線をまたぐ図形に注意**（`unwrap`）。経度が +180 と −180 を行き来する輪は、
そのまま描くと地図を真横に横断します。隣り合う点の差が180度を超えたら360度ずらして
1本の連続した輪に直したうえで切り取ります。フィジー付近とシベリアで実際に発生していました。

世界地図の範囲は `[-130,-46,162,62]` です。高緯度まで入れるとメルカトルの引き伸ばしで
北極周辺が大きく歪むので、競馬場のある緯度に合わせて切っています。

棒の色は年史のグラフと同じ検証済みの系列色（`--s1`〜`--s3`）です。
ライトモードの緑はコントラストが3:1を下回るため、**必ず同じ数値を持つ表を横に併設**しています。

### 読み込んでいるシート

| シート | gid | 用途 |
|---|---|---|
| 現役馬 | 0 | 馬マスタ |
| 引退馬 | 1637103436 | 馬マスタ |
| 繁殖牝馬 | 482152832 | 馬マスタ |
| 種牡馬 | 495363124 | 馬マスタ |
| レース結果入力(登録済) | 1732161958 | 全戦績 |
| フォームの回答 2 | 1758942740 | 毛色・調教師・所属の補完 |
| 騎手マスタ | 708548464 | 騎手名の置き換え |
| 海外騎手マスタ | 776150858 | 同上（姓で照合） |
| 調教師マスタ | 1019895905 | 調教師名の置き換え |
| 海外調教師マスタ | 2023682065 | 同上（姓で照合） |
| レースマスタ | 769926424 | 重賞レース一覧と施行条件 |

「レース結果入力」(gid=2145686754) と「設定」は読み込んでいません。

### 名前マスタの仕組み

`名前`（ゲーム内表記）→ `変更後`（実在の人名）の対応表として読み込みます。

- **変更後が空欄なら、ゲーム内表記のまま表示**します（オリジナルの架空キャラ）
- 海外シートはフルネーム登録、レース結果は姓のみの表記なので、**姓で照合**します
- 海外シートの `変更後` は「日本語名 + 改行 + 英語名」を想定し、1行目を表示名、2行目を併記に使います
- 姓が重複する場合は `SURNAME_PICK` で指定します（現在は `トスカロ` → `セサール トスカロ`）
- レース結果側の表記ゆれは `PERSON_ALIAS` で吸収します（現在は `ディハサン` → `ディサハン`）

### シートのフィルターに注意

**スプレッドシートにフィルターを掛けたままにすると、アプリもその絞り込み結果しか読み込みません。**
gvizは表示中の行だけを返すためです。データを確認するために絞り込んだあとは、
フィルターを解除してください。

実際に、レース結果を絞り込んだまま放置して2,775走が19走として読み込まれたことがあります。
そのまま採用すると誤った集計をキャッシュしてしまうため、次の2段構えで防いでいます。

- `fetchOne()` … HTTPエラー・HTML応答・空応答なら600ms待って1回だけ再試行する
- `looksTruncated()` … 前回のキャッシュと比べて行数が6割未満のシートがあれば採用せず、
  前回のデータを保持して右上に「データが大幅に減少」と表示する（カーソルを乗せると理由が出ます）

行を大量に削除した場合も同じ警告が出ます。その状態で最新を読み込みたいときは、
ブラウザのlocalStorageから `wp-cache-v2` を消してください。

### チャートの系列色

年史ページの積み上げ棒グラフは、アプリの地域色（`--jp` / `--us` / `--eu`）ではなく
専用の `--s1` / `--s2` / `--s3` を使います。地域色は青と紫が重度色覚多様性で
ΔE 1.7、通常視でも11.8（基準15未満）と識別できず、グラフでは使えないためです。

| | ライト | ダーク |
|---|---|---|
| `--s1` 日本 | `#2a78d6` | `#3987e5` |
| `--s2` アメリカ | `#eb6834` | `#d95926` |
| `--s3` 欧州 | `#1baf7a` | `#199e70` |

全ペアでCVD ΔE 9.2以上（ライト）／9.4以上（ダーク）、通常視24.0／20.9を検証済みです。
ライトの緑はコントラスト2.82と3:1を下回るため、同じ数値を持つ表を必ず併設してください。

### 格の色（`--g1` / `--g2` / `--g3` / `--gx`）

地図の「内訳: 格別」で使う、G1＝青・G2＝赤・G3＝緑の配色です。

| | ライト | ダーク |
|---|---|---|
| `--g1` G1・JpnⅠ | `#2b6cd4` | `#4d8fe3` |
| `--g2` G2・JpnⅡ | `#b83226` | `#bf3d2e` |
| `--g3` G3・JpnⅢ | `#66b84f` | `#4aa350` |
| `--gx` その他 | `#8a9490` | `#7c8a82` |

**赤と緑は色覚多様性で最も混同しやすい組み合わせ**なので、明度を大きく離して逃がしています
（赤を暗く、緑を明るく）。素直な赤緑（例 `#d6402f` と `#3f9e3f`）は重度2型でΔE 2.9と
判別不能になり検証に落ちます。現在の値は全ペアでCVD ΔE 8.5以上（ライト17.4／ダーク8.5）、
通常視23.9以上です。**この3色を触るときは必ず検証スクリプトを通してください。**

```bash
node scripts/validate_palette.js "#2b6cd4,#b83226,#66b84f" --mode light --pairs all
```

ライトの緑はコントラスト2.4で3:1を下回ります。地図の横に同じ数値の表を出しているのが
その代替手段なので、表を消さないでください。

### 入力ページ（サイトからの書き込み）

ナビの「入力」から、スプレッドシートを開かずに出走登録と結果入力ができます。**スマホからも使えます。**

書き込みは GAS のウェブアプリ経由です。静的サイトは自分ではシートに書けないためです。

```
サイト ──POST──► GAS ウェブアプリ（doPost）──► スプレッドシート
       ◄─gviz──── 読み取りは今までどおり直接
```

- **合言葉** … 端末の `localStorage`（`wp-api-token`）に保存。**コードにもリポジトリにも入りません**。
  スプレッドシートの「WPツール → Webアプリの合言葉を作り直す」で作り、初回だけ貼り付けます
- **CORS** … `Content-Type: text/plain` で送るとプリフライトが起きないので、GASへ素直に届きます。
  JSONで送りたくなっても、このヘッダは変えないでください
- **URL** … `API_URL` にそのまま書いています。リポジトリが公開なので誰でも見られますが、
  合言葉がないと `{"ok":false,"error":"合言葉が違います"}` しか返りません

GAS側の `doPost` が受ける `action` は `ping` / `init` / `searchRaces` / `registerEntries` /
`getPending` / `saveResults` / `commit` / `horseOptions` / `registerHorse` の9つです。
サイドバーと同じ関数を呼んでいるので、**入力の仕組みはサイドバーとサイトで共通**です。

タブは3つあります。

| タブ | いつ使うか |
|---|---|
| ① 出走登録 | レース前。人気・着順・タイム・賞金は空のまま行を作る |
| ② 結果入力 | プレイ後。着順を入れると賞金が自動で決まる。本登録もここ |
| ③ 新馬登録 | 新しい馬を現役馬シートへ。IDは自動で振られる |

**③はGoogleフォームの置き換えです。** 父・母・調教師を既存の名前から補完できるのが利点で、
これはフォームでは作れませんでした。実際 `ALIAS` には
`二ホンピロムーテー → ニホンピロムーテー`（漢数字の二とカタカナのニ）のような、
目視では気づけない取り違えの補正が入っています。候補から選べばこの種の事故は起きません。

同じ馬名が既にあると、押す前に注意が出ます。それでも登録したい場合は、
GASが一度断ったうえで、もう一度押すと通ります（`force`）。
登録すると生年・調教師・所属は残るので、同期の馬を続けて入れやすくしています。

**GASのコードを直したら、デプロイし直さないと反映されません。**
「デプロイ → デプロイを管理 → 鉛筆 → バージョン『新バージョン』→ デプロイ」ならURLは変わりません。

本登録が終わると `reloadData()` でシートを読み直し、サイトの各ページにその場で反映します。

### 検索エンジンに載せない設定

このサイトは「リンクを知っている人だけが見る」前提です。検索結果に出ないよう2段構えにしています。

- `index.html` の `<meta name="robots" content="noindex, nofollow, noarchive, noimageindex">`
- `robots.txt` の `Disallow: /`

`<meta name="referrer" content="no-referrer">` も入れています。サイトから外部へ移動したとき、
**このサイトのURLを相手に渡さない**ためです。スプレッドシートのgvizを叩くときも送られません。

**できること・できないことをはっきりさせておきます。**

- できる … 検索エンジンの結果に出さない
- **できない** … URLを知っている人のアクセスを防ぐこと

GitHub Pages は静的配信なのでログインをかけられません。またリポジトリが公開なので、
**スプレッドシートのIDはソースを読めば分かります**。シート自体がリンク共有で公開されている以上、
そこは変わりません。個人のゲーム記録なので実害は小さいという判断です。

本当に閉じたい場合は、Google アカウントでのログインを挟む方式（OAuth）に切り替えて、
スプレッドシートを非公開にする必要があります。

### `headers=1` について

gvizは指定がないとヘッダー行数を自動推測します。文字列だけの列が続くシートでは
**先頭の数百行をまとめてヘッダーと誤判定し、1行に連結してしまう**ことがあります。
実際に騎手マスタ（444行）が79行に見える現象が起きました。
`sheetURL()` で全シートに `headers=1` を付けているので、消さないでください。

---

## 表彰ルールの要約

- **複合スコア** ＝ 獲得賞金 ＋ G1勝ち × 20,000 ＋ JpnⅠ勝ち × 10,000
  ＋ G1 2着 × 10,000 ＋ G1 3着 × 4,000（JpnⅠは半分）＋ 三冠ボーナス
- **表彰対象は所有馬のみ**。他厩舎の馬は成績の一部しか判明しないため候補に入れず、
  各地域の「他厩舎のG1勝ち馬」に参考として掲載する

### 他厩舎馬の賞金の推定

勝ち馬はレース結果のタイム欄の `(馬名)` から特定します。1着賞金は3段階で求めます。

1. **自厩舎が勝った** … その賞金がそのまま1着賞金
2. **自厩舎が2〜5着** … `RATE` の着順別賞金率で逆算する
   （中央・海外＝2着40% / 3着25% / 4着15% / 5着10%、
   　地方交流＝2着35% / 3着20% / 4着10% / 5着5%）
3. **自厩舎が6着以下で賞金0** … 同じレース名の他年度の1着賞金から推定する。
   最頻値を優先し、同数なら年が近いものを採る

実測では、レース種557のうち538は1着賞金が年によらず同額でした。
残り19種は端数のずれか、格が変わったレースです。最頻値を使うのはこの外れ値対策です。
- **最優秀馬** … G1（またはJpnⅠ）を1勝以上していることが必須
- **世代別（2歳・3歳・4歳上／牡牝）** … いずれもG1（またはJpnⅠ）勝ちが必須
- **最優秀スプリンター／最優秀マイラー** … **国内部門のみ**（`HAS_SPRINT_MILE`）。
  海外は年によってマイル路線を使わないことがあり、部門が空振りするため設けていない。
  条件は次の3つをすべて満たすこと。
  - その年に**3歳以上**であること
  - **芝**の該当距離帯（スプリンター＝1400m以下／マイラー＝1401〜1600m）で**G1を勝つ**こと
  - その勝ち鞍に**古馬混合戦**が1つ以上含まれること。2歳・3歳限定のG1は`AGE_LIMITED`で
    除外する（阪神JF・朝日杯FS・桜花賞・NHKマイルC）。これらは世代別部門で扱う

  実質的な対象レースは、スプリンターが高松宮記念・スプリンターズS、
  マイラーが安田記念・マイルCS・ヴィクトリアマイル、および海外の古馬芝短距離G1です。
  複合スコアの集計も**芝の該当距離帯に出走したぶんだけ**を対象にします
  （ダートのフェブラリーS・かしわ記念などはダート部門で扱う）。
  1700m以上は中距離寄りなのでマイラーには含めません。
- **最優秀ダートホース** … 日本とアメリカのみ。欧州部門にはなし
- **海外部門（アメリカ・欧州）** … G1 2勝以上、またはG1 1勝かつ年間賞金10,000以上

### 三冠ボーナス（`CROWNS`）

レース名だけで判定するため格は問いません（サラトガオークス招待SはG2、ジョッキークラブオークスはG3）。
**通年成績にのみ加算**し、スプリンター・マイラー・ダートの部分集計には乗せません。重複して達成した場合は合算します。

| 称号 | 加点 | 対象レース |
|---|---|---|
| クラシック三冠 | 60,000 | 皐月賞・東京優駿・菊花賞 |
| 米国三冠 | 60,000 | ケンタッキーダービー・プリークネスS・ベルモントS |
| 欧州三冠 | 60,000 | 英2000ギニー・英ダービー・英セントレジャー |
| 春古馬三冠 | 50,000 | 大阪杯・天皇賞(春)・宝塚記念 |
| 秋古馬三冠 | 50,000 | 天皇賞(秋)・ジャパンカップ・有馬記念 |
| トリプルティアラ | 50,000 | エイコーンS・コーチングクラブアメリカンオークス・アラバマS |
| 米国芝三冠 | 40,000 | ベルモントダービー招待S・サラトガダービー招待S・ジョッキークラブダービー招待S |
| 牝馬クラシック三冠 | 40,000 | 桜花賞・優駿牝馬・秋華賞 |
| 米国芝牝馬三冠 | 30,000 | ベルモントオークス招待S・サラトガオークス招待S・ジョッキークラブオークス |
| 香港短距離三冠 | 30,000 | センテナリースプリントC・チェアマンズスプリントプライズ・香港スプリント |
| 古馬王道完全制覇 | 100,000 | 春古馬三冠＋秋古馬三冠の6レース |
| 春秋マイルG1制覇 | **0**（称号のみ） | 安田記念・マイルCS |
| 春秋スプリントG1制覇 | **0**（称号のみ） | 高松宮記念・スプリンターズS |
| 天皇賞春秋連覇 | **0**（称号のみ） | 天皇賞(春)・天皇賞(秋) |
| 春秋グランプリ連覇 | **0**（称号のみ） | 宝塚記念・有馬記念 |
| 牝馬G1完全制覇 | **0**（実績表示のみ） | 阪神JF・桜花賞・優駿牝馬・秋華賞・エリザベス女王杯・ヴィクトリアマイル |

牝馬G1完全制覇のみ `career:true` でキャリア通算で判定し、最後の1冠を獲った年に紐づけます。
他はすべて同一年内での達成が条件です。

**無敗での達成**（デビューから最後の1冠まで一度も2着以下がない）は、
その称号の点数にさらに `PT_UNDEFEATED`（20,000）を上乗せし、称号名を「無敗の〜」と表示します。
加点のある称号だけが対象で、二冠系・牝馬G1完全制覇には無敗判定を行いません。

二冠系（2レース制覇）を加点なしにしているのは、その2勝が既にG1勝ちとして
20,000ずつ計上されており、3レースの三冠が30,000〜60,000であることと釣り合わないためです。
称号としては年度表彰とカルテに表示します。

G1の2着・3着への加点（`PT_2ND` / `PT_3RD`）は、勝ち切れなかったが大レースで
好走した価値を拾うためのものです。これは部分集計（スプリンター・マイラー・ダート）にも反映されます。
